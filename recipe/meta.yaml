{% set name = "openvscode-server" %}
{% set version = "1.75.1" %}

package:
  name: {{ name|lower }}
  version: {{ version|replace("-", "_") }}

source:
  url: https://github.com/gitpod-io/openvscode-server/archive/refs/tags/openvscode-server-v{{ version }}.tar.gz
  sha256: 1c723da6e75a374f9185fba5f5aa65be78b35564210153e4f1253ec9f38e71e2
  folder: src
  patches:
    - 0001-Allow-running-on-subdir.patch

build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - make
    - pkg-config
    - sysroot_linux-64 2.17  # [build_platform == 'linux-64']
    - nodejs 16.*
    # We might need to ensure that we recompile all third-party deps to support OpenSSL 3+
    - openssl <3
    - glib         # [linux]
    - libsecret    # [linux]
    - libxkbfile   # [linux]
    - xorg-libx11  # [linux]
  host:
    - nodejs 16.*
    # We might need to ensure that we recompile all third-party deps to support OpenSSL 3+
    - openssl <3
    - yarn <2
    - ripgrep
    - glib         # [linux]
    - libsecret    # [linux]
    - libxkbfile   # [linux]
    - xorg-libx11  # [linux]
  run:
    - nodejs 16.*
    - ripgrep

test:
  commands:
    - openvscode-server --help

about:
  home: https://github.com/gitpod-io/openvscode-server
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE.txt
    - ThirdPartyNotices.txt
  summary: Run upstream VS Code on a remote machine with access through a modern web browser from any device, anywhere.

extra:
  recipe-maintainers:
    - xhochy
    - jtilly
    - janjagusch
